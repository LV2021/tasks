#include <iostream>
#include<string>

int main()
{
    std::cout << "Enter string" << std::endl;
    std::string str;
    getline(std::cin, str);
    for (int i = 0; str[i] != '\0'; ++i) {
        if (str[i] > 90) {
            str[i] = str[i] - 32;
        }

    }
    std::cout << str;
